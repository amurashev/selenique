import PageEntry, { metadata } from "../[lang]/prompt-books/page";

export { metadata };
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export default async (params: any) => <PageEntry {...params} />;
